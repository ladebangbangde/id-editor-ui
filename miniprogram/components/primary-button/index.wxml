<button
  class="btn btn-{{type}} {{disabled || loading ? 'disabled' : ''}}"
  catchtap="handleTap"
  loading="{{loading}}"
  disabled="{{disabled||loading}}"
  hover-class="btn-hover"
  hover-stay-time="70"
>
  <text class="btn-label">{{loading ? '处理中...' : text}}</text>
</button>
