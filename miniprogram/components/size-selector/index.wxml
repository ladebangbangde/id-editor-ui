<view class="size-list">
  <block wx:for="{{options}}" wx:key="value">
    <view
      class="size-item {{item.value === value ? 'active' : ''}}"
      data-value="{{item.value}}"
      bindtap="handleTap"
    >
      <text class="size-name">{{item.label}}</text>
    </view>
  </block>
</view>
